{
  "extends": "./tsconfig.json", // eslint设置了project地址, 则必须设置include(即eslint作用的范围)
  "include": [
    "config/**/*.js",
    "scripts/**/*.js",
    ".eslintrc.js",
    "lint-staged.config.js",
    "src/**/*.ts",
    "src/**/*.tsx",
  ]
}